[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "code-agent-from-scratch"
version = "0.1.0"
description = "A code agent from scratch to learn how a code agent works."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["src"]
